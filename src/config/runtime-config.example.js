// Copy this file to runtime-config.local.js for local-only credentials/configuration.
// Never commit the copied file or put a Tianditu token in map-config.js or HTML.
window.APP_RUNTIME_CONFIG = {
  tiandituToken: '',
};
