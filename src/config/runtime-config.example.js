// Optional non-sensitive local override. Never put ORS credentials in browser code.
// Never commit the copied file or put a Tianditu token in map-config.js or HTML.
window.APP_RUNTIME_CONFIG = {
  apiBaseUrl: 'http://127.0.0.1:8000/api/v1',
  tiandituToken: '',
};
